package com.example.crashcourse.ui.add

/**
 * Common Enum for Face Capture Actions across Azura UI
 */
enum class CaptureMode { 
    EMBEDDING, // Digunakan untuk pendaftaran biometric
    PHOTO      // Digunakan untuk profil user/murid
}