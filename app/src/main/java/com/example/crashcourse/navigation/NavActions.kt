package com.example.crashcourse.navigation

/**
 * 💡 THE SOLID SOLUTION: Standardized Navigation Callbacks
 * This object ensures that the NavGraph and the UI Screens 
 * always use the exact same parameter names.
 */
object NavActions {
    const val ON_BACK = "onNavigateBack"
    const val ON_SUCCESS = "onUpdateSuccess" 
}